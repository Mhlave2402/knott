# FILE: .env.example

APP_NAME=Knott
APP_ENV=local
APP_KEY=
APP_DEBUG=true
APP_TIMEZONE=UTC
APP_URL=http://localhost

APP_LOCALE=en
APP_FALLBACK_LOCALE=en
APP_FAKER_LOCALE=en_US

APP_MAINTENANCE_DRIVER=file
# APP_MAINTENANCE_STORE=database

# PHP Configuration
BCRYPT_ROUNDS=12

LOG_CHANNEL=stack
LOG_STACK=single
LOG_DEPRECATIONS_CHANNEL=null
LOG_LEVEL=debug

# Database Configuration (PostgreSQL)
DB_CONNECTION=mysql
DB_HOST=127.0.0.1
DB_PORT=3306
DB_DATABASE=knott
DB_USERNAME=root
DB_PASSWORD=Mhlave0034290@

# Session Configuration
SESSION_DRIVER=database
SESSION_LIFETIME=120
SESSION_ENCRYPT=false
SESSION_PATH=/
SESSION_DOMAIN=null

# Broadcasting
BROADCAST_CONNECTION=log
CACHE_STORE=database
FILESYSTEM_DISK=local
QUEUE_CONNECTION=database

# Mail Configuration (SendGrid recommended for production)
MAIL_MAILER=smtp
MAIL_HOST=smtp.sendgrid.net
MAIL_PORT=587
MAIL_USERNAME=apikey
MAIL_PASSWORD=your_sendgrid_api_key
MAIL_ENCRYPTION=tls
MAIL_FROM_ADDRESS="hello@knott.co.za"
MAIL_FROM_NAME="${APP_NAME}"

# AI Services
GOOGLE_GEMINI_API_KEY=AIzaSyAcZeFlRPZf3aZOAH_uX9jiUlgYMJfv5ik

# Payment Gateways (Phase 4)
STRIPE_KEY=pk_test_your_stripe_publishable_key
STRIPE_SECRET=sk_test_your_stripe_secret_key
STRIPE_WEBHOOK_SECRET=whsec_your_stripe_webhook_secret

# PayFast (South African Payment Gateway)
PAYFAST_MERCHANT_ID=your_payfast_merchant_id
PAYFAST_MERCHANT_KEY=your_payfast_merchant_key
PAYFAST_PASSPHRASE=your_payfast_passphrase
PAYFAST_SANDBOX=true

# Yoco (Alternative SA Payment Gateway)
YOCO_SECRET_KEY=sk_test_your_yoco_secret_key
YOCO_PUBLIC_KEY=pk_test_your_yoco_public_key

# File Storage (AWS S3 for production)
AWS_ACCESS_KEY_ID=
AWS_SECRET_ACCESS_KEY=
AWS_DEFAULT_REGION=us-east-1
AWS_BUCKET=
AWS_USE_PATH_STYLE_ENDPOINT=false

# Pusher (for real-time notifications)
PUSHER_APP_ID=
PUSHER_APP_KEY=
PUSHER_APP_SECRET=
PUSHER_HOST=
PUSHER_PORT=443
PUSHER_SCHEME=https
PUSHER_APP_CLUSTER=mt1

# Redis (for caching and queues in production)
REDIS_CLIENT=phpredis
REDIS_HOST=127.0.0.1
REDIS_PASSWORD=null
REDIS_PORT=6379

# Vite Configuration
VITE_APP_NAME="${APP_NAME}"
VITE_PUSHER_APP_KEY="${PUSHER_APP_KEY}"
VITE_PUSHER_HOST="${PUSHER_HOST}"
VITE_PUSHER_PORT="${PUSHER_PORT}"
VITE_PUSHER_SCHEME="${PUSHER_SCHEME}"
VITE_PUSHER_APP_CLUSTER="${PUSHER_APP_CLUSTER}"

# Application Features (Enable/Disable)
FEATURE_VENDOR_REGISTRATION=true
FEATURE_AI_MATCHING=true
FEATURE_GIFT_WELLS=true
FEATURE_COMPETITIONS=true
FEATURE_NEGOTIATORS=true

# Platform Settings
PLATFORM_COMMISSION_RATE=0.05
GIFT_WELL_GUEST_FEE=0.10
GIFT_WELL_WITHDRAWAL_FEE=0.0395

# Social Media (for sharing and authentication)
FACEBOOK_CLIENT_ID=
FACEBOOK_CLIENT_SECRET=
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=

# Analytics and Monitoring
GOOGLE_ANALYTICS_ID=
MIXPANEL_TOKEN=
SENTRY_LARAVEL_DSN=

# Content Delivery Network
CDN_URL=